#!/bin/bash

for file in *; do
    if [ -f "$file" ]; then
        if [[ "$file" == *.* ]]; then
            extension="${file##*.}"
            mkdir -p "$extension"
            mv "$file" "$extension/"
        fi
    fi
done
